services:

  # ─── OWASP ZAP ───────────────────────────────────────
  zap:
    image: ghcr.io/zaproxy/zaproxy:stable
    command: >
      zap.sh -daemon
      -host 0.0.0.0 -port 8080
      -config api.addrs.addr.name=.*
      -config api.addrs.addr.regex=true
      -config api.disablekey=true
    deploy:
      resources:
        limits:
          memory: 2g
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8080/JSON/core/view/version/"]
      interval: 20s
      timeout: 10s
      retries: 5
      start_period: 60s
    restart: unless-stopped
    networks:
      - loopsec-net

  # ─── FastAPI backend ──────────────────────────────────
  api:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env.prod
    environment:
      ZAP_HOST: http://zap:8080
    volumes:
      - loopsec-data:/data        # SQLite + work files persist across restarts
    depends_on:
      zap:
        condition: service_healthy
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8000/health"]
      interval: 15s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    networks:
      - loopsec-net

  # ─── React frontend (nginx) ───────────────────────────
  ui:
    build:
      context: ../LoopSec-UI
      dockerfile: Dockerfile
      args:
        # Replace with your EC2 public DNS or Elastic IP after launch
        VITE_API_URL: ${VITE_API_URL:-http://localhost:8000}
    ports:
      - "80:80"
    depends_on:
      - api
    restart: unless-stopped
    networks:
      - loopsec-net

  # ─── Caddy reverse proxy (HTTPS + routing) ───────────
  caddy:
    image: caddy:alpine
    ports:
      - "443:443"
      - "80:80"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      - api
      - ui
    restart: unless-stopped
    networks:
      - loopsec-net

volumes:
  loopsec-data:
  caddy-data:
  caddy-config:

networks:
  loopsec-net:
    driver: bridge
